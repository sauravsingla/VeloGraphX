# Observability and explainability

`ExecutionEstimate`, `ExecutionPlan`, `choose_execution`, and `explain` provide the first observable planner surface. Plans report incremental and full estimated work plus the selected mode and reason. Dynamic graphs expose version, edge count and delta ratio. Future benchmark-driven calibration will add measured histories rather than opaque constants.
