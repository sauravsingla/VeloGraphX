# Benchmarks

The repository contains static and dynamic smoke benchmarks. Research-quality claims require the benchmark methodology in `docs/benchmark-methodology.md`, multiple repetitions, hardware metadata, public datasets, update fractions from 0.0001% through 10%, memory accounting, and full-recompute baselines. Smoke benchmark output is not a published performance result.
