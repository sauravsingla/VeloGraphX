name: Documentation Pages

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'Doxyfile'
      - 'docs/**'
      - 'include/velographx/**'
      - 'python/README.md'
      - 'README.md'
      - 'REPRODUCIBILITY.md'
      - 'PAPER.md'
      - 'tools/build_docs_seo.py'
      - '.github/workflows/docs-pages.yml'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Doxygen
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends doxygen

      - name: Build C++ API documentation
        run: |
          mkdir -p build/docs
          doxygen Doxyfile
          test -f build/docs/html/index.html

      - name: Add and validate search and social metadata
        run: |
          python3 tools/build_docs_seo.py build/docs/html
          touch build/docs/html/.nojekyll

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: build/docs/html

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 10
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
